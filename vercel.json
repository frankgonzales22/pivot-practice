
{
  "rewrites": [
    {
      "source": "/(.*)",      // Match any incoming URL
      "destination": "/"      // Rewrite it to the root "/"
    },
    {
      "source": "/DisplayCharts", // Match incoming URL "/DisplayCharts"
      "destination": "/DisplayCharts" // Rewrite it to "/DisplayCharts"
    }
  ]
}
